# LAN Host Options Bottom

Goal: Move LAN host game options such as co-op QoL sliders to the bottom of the host setup list.

- [x] Read project instructions and locate the LAN host view.
- [x] Reorder the host option controls to render after the main host list content.
- [x] Run scoped formatting and focused tests/build checks.
