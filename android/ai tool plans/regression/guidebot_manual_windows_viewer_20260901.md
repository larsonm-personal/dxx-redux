# GuideBot manual Windows viewer repair

## Goal

Make the manual mission/level browser render immediately and launch a visible
Windows game-engine route confirmation, with no visible helper PowerShell
window.

## Phases

- [x] Compare the current Android headed path with the Windows input-demo host runner
- [x] Add a desktop headed route-confirmation entry point or mode using the shared deterministic controller
- [x] Change the manual browser to launch the Windows game window and hide its helper shell
- [x] Make the search TUI paint before its first keypress
- [x] Add launch-command and initial-render regression coverage
- [x] Run a focused Obsidian level 1 visible smoke test, quality checks, and Windows build
