# Mission Archive Count Progress

## Goal
- Show count-based mission archive import progress as `done / total` instead of byte units like `4 B / 8 B`.

## Steps
- [x] Locate the mission archive import progress formatter.
- [x] Add count-aware formatting for non-byte mission archive tasks.
- [x] Add focused coverage and run scoped formatting/tests.
