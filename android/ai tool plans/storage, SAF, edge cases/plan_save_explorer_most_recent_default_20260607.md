# Save Explorer Most Recent Default

## Goal
Rename Save Explorer `Ten Recent` to `Most Recent`, move it to the leftmost mode, and make it the default mode when the dialog opens.

## Plan
- [completed] Update Save Explorer mode ordering, label, and default selection.
- [completed] Add a focused unit test for mode labels and default.
- [completed] Run scoped formatting and focused tests.
