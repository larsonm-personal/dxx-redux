# Expanded door and trigger routing regression work

User requests adding Lostlvls, EAF2, EAF and already-passing Bahagad, then pursuing tractable failures through general door/trigger-state improvements. Live/co-op logs will come from the user. Avoid difficult cases likely to encourage special cases; retain guided-missile skepticism and legacy demo compatibility

1. Add four missions to the shared manifest and verify discovery of17 missions/343 levels and master regeneration integration
2. Run the expanded corpus to establish current results, keeping Bahagad as a preservation check
3. Select tractable ordinary failures from the additions, diagnose from native evidence, and implement general shared fixes with focused repeated integration coverage
4. Verify appropriate builds, demos when movement/state changes warrant them, and full expanded corpus; publish verified simulation reports and record remaining limitations

Initial checked-in additions: Lostlvls18/25 passing (five timeouts, one failure, one guided unsupported); EAF2 9/10; EAF3/5; Bahagad10/10. These historical statuses need fresh verification before selecting a fix. No implementation in headers or changes to original FVI physics

Manifest and master-regeneration tests pass, as does scoped formatting. Fresh expanded17_baseline sweep completes343 levels:324 OK /7 timeout /7 failed /5 unsupported, matching every checked-in status. Bahagad remains10/10. Current inspection candidates are EAF L2 (no initial goal) and EAF2 L2 (all three keys then no goal). EAF L5 reports missing exit and is left aside as a structural case. A temporary native STATE-PROBE in route_confirmation.cpp dumps failure-time objects, graph and triggers; clean source snapshot temp/expanded17_confirmation_before_probe.cpp. Remove this diagnostic before accepting any fix

EAF2 L2 evidence: reactor569 sits beyond a permanent grate; the inaccessible firing area is reached through ordinary opening triggers. Center-only visibility search found no candidate. Existing detailed visibility sampling found seven ordinary firing poses (segments553,560,559,563,564,567,568) in the optimistic access graph. Added an expansion-only fallback to prepare access to a verified firing pose with the existing move_to_target prerequisite resolver. It preserves the original target and restores speculative state on failure; no global physics or guided classification change. All STATE/FIRING-ACCESS diagnostic probes removed. Candidate build temp/expanded17_firing_access_build.log; focused physical verification pending

The firing-access candidate physically confirms EAF2 L2 twice at6924 frames: switches3/4/5 and all key carriers, then ordinary access switches0/1/2, reactor shot from553 into569, andexit. Fresh metadata now produces a complete ordinary route. New test_eaf2_reactor_access.ps1 regenerates isolated metadata and repeats physical completion, checking exact objective order, the verified outside-grate reactor shot, no guided requirement, deterministic results and normalizedOK. Initial test staging omitted the adjacent archive; corrected the test to copy EAF2.zip alongside isolated metadata. Final integration passes

Full expanded17_firing_sweep retains all324 prior passes. Three formerly failed levels physically confirm but report route_mismatch only because saved metadata is partial: EAF2 L2(6924frames), Vertigo secret-2(722frames, switch14/reactor/exit), Vignettes secret-1(1094frames, hidden door/red key/open door/reactor/exit). No other status changes. EAF L2 remains unresolved and is not being forced into a special solution. Bahagad10/10 retained. Native49tests pass. Cache generations advanced together to30 to invalidate earlier partial plans. Final both-engine build, metadata refresh for the three affected missions, repeat checks, demo compatibility and Android build are pending

Final cache-generation30 Windows builds pass for both engines, with49/49 D2 CTest cases passing (D1 has no configured CTest suite). Final native metadata regeneration changed exactly EAF2 L2, Vertigo secret-2, and Vignettes secret-1 to complete routes; no other level records changed. Published those three generated metadata files after backing up prior inputs at temp/expanded17_before_publish. Final343-level WriteRegression sweep is running at android/temp/expanded17_final_sweep. Additional Vertigo secret-2 and Vignettes secret-1 physical repeats both match complete raw JSON at722 and1094frames respectively. EAF2 integration passes at6924frames. Final demo and Android checks remain pending

Final publication sweep completed343/343 selected levels without infrastructure failures. Published counts327 OK /7 timeout /4 failed /5 unsupported. Every previous324 passing identity is retained; Bahagad remains10/10. Exactlythree status improvements: EAF2 L2, Vertigo secret-2, Vignettes secret-1. Metadata and all17 simulation reports are refreshed. Comparison temp/expanded17_final_comparison.json; authoritative run android/temp/expanded17_final_sweep. Final15 committed demos pass without expectation changes (temp/expanded17_final_demos.log), and schema tests pass (temp/expanded17_final_schema.log). Allfour FVI files still match the pre-correction revision. Android native/JVM validation remains in progress

## Completion audit

- All four requested additions are in the shared manifest: Lostlvls25 levels, EAF2 10, EAF5, Bahagad10. Discovery proves17missions/343levels; master regeneration integration and scoped formatting pass
- Tractable new-mission investigation selected EAF2 L2 after screening EAF and Lostlvls failures. A general shared planner fix resolves ordinary access prerequisites for an off-center firing pose. Existing move_to_target state/dependency handling is reused; no mission identifiers, hardcoded geometry, global collision changes or guided workaround are introduced. The same fix completes two additional ordinary routes in Vertigo and Vignettes
- New end-to-end EAF2 test verifies freshly generated metadata, absence of guided requirements, actual outside-grate reactor shot, full objective sequence and two identical completions. Vertigo and Vignettes repeats also match complete raw JSON. No difficult structural EAF or other deferred failures were forced into exceptions
- Final343-level publication proves327 OK /7 timeout /4 failed /5 unsupported, preserving all324 prior passes and Bahagad10/10. Exactlythree improvements and no route mismatches. Three affected metadata files and17 simulation reports are updated; all other level metadata records in the three regenerated missions are unchanged
- Both Windows engines build;49 D2 native tests pass. D1 has no registered CTest suite. All15 committed demo fixtures pass unchanged. Schema, discovery, master regeneration, scoped formatter and diff checks pass
- Final Android externalNativeBuildDebug passes arm64-v8a, armeabi-v7a and x86_64;19 targeted launcher scheduling/monitor tests pass (temp/expanded17_android_final.log). Native and JVM cache generations both30
- No diagnostic probes remain. User will supply future live/co-op logs; no on-device reproduction or exhaustive live-play validation is claimed. Remaining16 unresolved levels are retained in the expanded set and left for further tractable work, consistent with the request to avoid forcing the hardest cases

The requested expansion and this batch of general routing improvements are complete
