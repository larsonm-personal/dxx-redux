# Controller-only menu buttons overlay

## Goal
Scope and, if straightforward, implement the controller-only default touch overlay behavior:

- add a built-in touch preset containing only the two menu buttons
- select that preset by default on a new install when no touch interface is detected
- keep the overlay menu option active when hidden, but hide the menu buttons until the controller menu-selection switch activates the menus

## Plan
- [in progress] Trace the current touch preset definitions, default config loading, and touch-interface detection paths
- [pending] Identify what is already implemented and what still needs changes
- [pending] Make focused code changes if the implementation surface is clear
- [pending] Run scoped formatting and the most relevant available tests or compile checks
- [pending] Update this plan with completed work and validation notes

