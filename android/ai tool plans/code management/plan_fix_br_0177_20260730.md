# Fix BR-0177 tracklist parsing

## Plan

- [x] Recheck the live parser, all callers, related findings, and existing JSON helpers
- [x] Separate strict tracklist JSON parsing from comment-aware JSON5 parsing
- [x] Add focused regression coverage for legal string contents and malformed input
- [x] Run scoped code quality and relevant fingerprint tests
- [x] Finalize BR-0177 and move it with disposition evidence to the done ledger

## Result

- Strict tracklist JSON is parsed without comment or trailing-comma rewriting.
- Fingerprint/configuration JSON5 and extraction-regression JSON5 share one
  string-aware parser for the supported comment and trailing-comma subset.
- Focused hostile-string, malformed-comment, current-corpus, workflow,
  regeneration, and code-quality validation passed.
- BR-0177 is archived as fixed in the done ledger with disposition evidence.
