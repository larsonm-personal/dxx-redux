# Distinguish original Descent from the fan conversion

1. Rename the fan archive and its paired regression files to descent.fan_d2_conversion, retaining archive contents and all regression results
2. Update the data manifest and active test references, without rewriting historical reports
3. Include D1 missions in the live simulation browser, label their D1-in-D2 mode, and prioritize the original FirstStrike metadata identity
4. Test search, original/conversion identity, and paired archive discovery; run scoped code quality

The original remains FirstStrike.json / FirstStrike.simulation.json and uses the built-in descent.hog. No mission descriptors or routing behavior change
