# Test failures 20260826 223736

## Goal

Diagnose and fix the eight failures in `temp/test_reports/report_20260826_223736.md`.

## Plan

- [x] Read repository instructions and locate the report.
- [x] Extract failure summaries and referenced artifacts.
- [x] Trace each failure through logs, tests, and relevant source history/state.
- [x] Group related failures and report evidence-backed root causes and recommended next actions.

## Findings

- Gradle unit tests: `missionIntentClassificationLabel("both")` falls through to `Ambiguous`; the new label mapping and its test disagree.
- Level metadata benchmark and secret-area baseline: both expected artifacts predate the newly emitted level counts and mission-intent metadata.
- Android save/load dispatch: the test still expects `[auto] save`, while autosave descriptions intentionally gained a level suffix in `c20df419`.
- Unified content mission picker: the default file set retained Total Chaos (`5a994b073b9b63c8e9fabc04`), so the test's `content_entries[0]` assertion is not isolated.
- Launcher D-pad: the test reads any non-empty button list immediately after DPAD_CENTER, allowing the old main-page introspection to satisfy its poll before recomposition.
- Obsidian objective markers: the live route certifier introduced in `077d2780` stopped replacing an equal switch activation/aim position with a valid firing position. This is a gameplay route regression, not merely a stale assertion.
- LAN: the auto-provisioned second emulator missed the fixed SetupActivity readiness window; subsequent two-emulator tests passed, indicating an infrastructure/cold-start failure.

## Fix implementation

- [x] Restore a valid, deterministic firing position when a pending shoot-switch step has equal activation and aim positions.
- [x] Update stale unit and automation expectations for canonical mission intent and level-suffixed autosaves.
- [x] Make mission-picker assertions independent of content ordering and synchronize launcher D-pad navigation assertions.
- [x] Harden cold secondary-emulator SetupActivity readiness.
- [x] Regenerate and review metadata benchmark and secret-area baselines.
- [x] Run scoped formatting, host/unit tests, and focused emulator tests where available.

## Final Obsidian fixture fix

- [x] Confirm the level 3 semantic-route failure with focused certifier tracing.
- [x] Preserve bosses and key-carrying robots in the automation-only `clear_robots` helper.
- [x] Rebuild and rerun the focused level 3 Guide-Bot scenario.
- [x] Keep the path-parity probe from treating its scratch arrays as the live AI path pool.
- [x] Run scoped code quality and record the final result.

## Verification

- D2 native host unit tests: 43/43 passed, including the new equal switch-position certifier case.
- Full `:app:testDebugUnitTest` suite: passed with JDK 21 (in-process Kotlin compiler fallback).
- Level metadata benchmark: all nine fixtures matched regenerated digests.
- Secret-area baseline: matched with required assets.
- Unified content mission picker: passed all 34 launcher/game steps.
- Launcher D-pad: all five checks passed.
- Save/load dispatch: D1 passed all 56 steps. D2 passed all 58 steps after successful full-planner fallbacks began publishing a fresh valid certificate instead of retaining the failed canonical-reuse certificate.
- Obsidian: the originally failing level 1 switch guidance and firing sequence passed, as did level 2 switch coverage. The longer scenario now waits for the trigger-8 wall transition to start and finish and correctly retains the non-bypassable fly-through objective; level 1, launcher continuation, and level 2 all passed. Its next failure is a separate level 3 test-fixture problem: `clear_robots` deletes all 79 non-companion robots, including the blue/red key carriers and boss. A focused traced reproduction showed the canonical certifier rejecting step 1 (fly-through trigger 6 at segment 379) as currently unreachable (`blocking_reason=2`, 144 reachable segments), then the full planner failing with `red key missing`. With no semantic route, legacy escort selection sees the level's boss topology but finds no live boss object, so the route goal remains inactive. The assertion guard prevents the prior crash but does not make the mutated level solvable.
- LAN integration: passed end to end with an auto-provisioned second emulator and the 60-second SetupActivity readiness timeout.
- Debug APK assembly and scoped code-quality checks: passed.
- Post-format x86_64 Android native rebuild: passed.
- Final Obsidian fixture: `clear_robots` retained the three progression actors on level 3, the fly-through objective at segment 379 was selected, and the complete 196-step scenario passed through the remaining level 10 and level 7 coverage.
- The level 3 path comparison now verifies the intended semantic-route divergence plus live AI/RNG restoration. Its dedicated scratch paths no longer invoke global `Point_segs` safety insertion, which previously reset live AI paths.
- Final debug APK assembly, scoped code quality, and `git diff --check` passed. The repository quality wrapper intentionally excludes upstream `d2/` sources; a whole-file clang-format dry run reports the file's existing legacy formatting, outside the edited path-parity hunk.
