# Level 8 replay analysis 2026-05-09 202642 202756 202946

## Goal
- determine whether the three new D2 level 8 replay failures share the same upstream control-flow split
- start with the shortest demo and use the new awareness, chase-path, and follow-path owner logs as the first discriminator
- capture any divergence pattern that differs from the earlier 173157 demo so the next fix targets the real owner path

## Steps
- [done] rerun the three demos with state trace, rng trace, and replay debug log enabled
- [done] compare recorded vs replay rng traces for all three demos to find each first behavioral mismatch
- [done] inspect durable owner logs around the first mismatch frame for each demo and look for shared object/state patterns
- [in_progress] summarize whether the three failures collapse to one root cause or multiple distinct splits and decide which demo gives the clearest next owner hop

## Notes
- target demos:
  - `android/regression_demos/d2_descent2_level8_20260509_202756.dximdemo`
  - `android/regression_demos/d2_descent2_level8_20260509_202946.dximdemo`
  - `android/regression_demos/d2_descent2_level8_20260509_202642.dximdemo`
- the repo now has durable replay owner logging for:
  - awareness source and awareness event attribution
  - `AIM_CHASE_OBJECT` lost-visibility path creation gates
  - `AIM_FOLLOW_PATH` transitions, including the still-path kill gate
- first pass should check whether these new failures again involve robot 176 or whether a different owner path now diverges first
- first-pass results:
  - `202756` first visible state drift is motion at frame 199, but the new owner logs around frames 190-200 still show robot 176 stably in `mode=2 path_length=15`; this one does not immediately resemble the older frame-903 follow-path kill
  - `202946` first substantive mismatch window is frames 422-423: replay records a player Gauss hit from weapon obj 78 to robot 129, and the rng compare shifts from expected `ai.c:do_ai_frame` to actual `collide.c:collide_robot_and_weapon`; the first visible state drift is an extra robot kill and score increase at frame 423
  - `202642` first mismatch window is frames 1097-1098: expected reaches `aipath.c:create_random_xlate` for obj 90 while replay consumes the same rng value in `ai.c:do_ai_frame` for obj 165; owner logs show obj 90 path state jumping from `2/3` to `0/56` while obj 176 remains on a long mode-2 path
- refined `202946` finding after collision instrumentation:
  - frame 422 replay enters `collide_robot_and_weapon` for robot 129 and player Gauss weapon 78 in seg 315
  - the collision-resolution probe shows `damage=1638400`, `shields=1295407->-342993`, `died=1`, `gauss_gate=1`, `skip_ai_count=3`
  - this means the replay-only hit is not just an awareness or spin side effect; it directly kills robot 129 at frame 422, and the first rng split at call 626160 is the Gauss spin tail of that same collision
  - because frame-summary state still matches through frame 422 start, the upstream cause is now narrowed to off-summary weapon or robot local state leading into that Gauss collision, not player state or the older robot-176 follow-path gate
  - replay frame 419 creates weapon 78, frames 420-421 sequence it, and the new durable FVI probe now records the frame-422 sphere-check hit in `ai_schedule_probe.log` with `p0=(-4081700,-56416,4979894)`, `p1=(-5586055,-290855,5745385)`, `center_dist=780517`, `miss_delta=341714`, `d=477071`, `hit=1`
  - new redone demos will already carry `probe_weapon_life` frame events for player-owned shots, and now also carry `probe_fvi_weapon_robot` frame events from the owning FVI sphere check, so the next compare can distinguish shot-creation drift from later weapon-path drift without relying on console-only output
- provisional classification:
  - `202946` and `202642` are both upstream AI/collision ordering problems, but not the same exact local branch as the older `173157` replay
  - `202756` may be a different class entirely, or at least a later motion/physics consequence that is not yet explained by the current AI owner logs