# Test failure investigation 2026-08-30 14:04:05

## Scope

Diagnose the seven failures in `temp/test_reports/report_20260830_140405.md` without changing production code or test expectations.

## Plan

- [x] Read repository instructions and identify all failed tests
- [x] Inspect complete failure logs and associated artifacts
- [x] Trace failures to tests, implementation, and relevant recent changes
- [x] Classify independent regressions, stale baselines, flaky thresholds, and cascading infrastructure failures
- [x] Report findings and recommended focused reruns or fixes

## Findings

- The Counterstrike route assertion and secret-area baseline are deterministic expectation drift after the conditional shot-blocker route change in commit `83d4ea2c`.
- The GuideBot failure is a wall-clock performance threshold flake. A focused rerun passed with `max_tick_us=3169` instead of the failed run's `12820`.
- The launcher DPAD failure is a polling timeout race. A focused rerun reported timeout while its immediate diagnostic read showed the requested `Controller Mapping` focus.
- The metadata preemption test stalled inside analysis after passing recently in roughly 41 seconds. This is consistent with an intermittent emulator or worker scheduling stall, not a demonstrated functional regression.
- The all-extracts sample never began extraction. Both attempts timed out sending the introspection broadcast, and the emulator reported a 6077-second clock jump during recovery.
- The Mac SAF test failed its first seekable extraction but did not retain enough diagnostics to identify the cause. It should be rerun alone on a clean emulator before classifying it as a product regression.
