# Test failures 20260829 170300

- [x] Locate the report and read repository instructions
- [x] Classify the five failures and two timeouts from report artifacts
- [x] Trace each result to the responsible test, code, or environment condition
- [x] Summarize root causes, confidence, and recommended next actions

## Findings

- `test_launcher_dpad` and the D2 half of `test_ogl_runtime_texture_options_unified` still expect `Define Controls`, renamed to `Controller Mapping` in commit `8c3832cd`
- `test_input_demo_regressions_d1_graphics_canary` was slowed by ordinary replay diagnostics writing a 5 MB `ai_schedule_probe.log`; the same demo passed in 68 seconds in the primary run, then the canary exceeded 180 seconds
- `test_input_demo_regressions_d2` never reached a replay; host auto-rebuild configuration hung inside `vcpkg install` until the 900 second suite timeout
- `test_level_metadata_benchmark` suffered host process-launch delays while analyzer-reported work remained 0.00 to 0.02 seconds; it passed in 36 seconds earlier the same day
- `test_android_saveload_dispatch_unified` intermittently hung in D1 immediately after the final quick-load request; the identical D1 sequence passed earlier the same day and D2 passed in this run
- `test_obsidian_level7_switch_guidance` selected the expected shoot-switch goal and trigger 7 but targeted segment 102 instead of the expected activation segment 304; this is the only direct gameplay/route regression in the report

## Implementation

- [ ] Update launcher tests for the `Controller Mapping` label
- [ ] Gate ordinary replay diagnostics so regression runs do not write hot-path probe logs
- [ ] Make quick-load automation survive the game-window rebuild
- [ ] Bound and recover host dependency configuration used by demo auto-rebuilds
- [ ] Restore the Obsidian switch target and verify metadata benchmark timing
- [ ] Run scoped format/lint plus focused and aggregate tests
