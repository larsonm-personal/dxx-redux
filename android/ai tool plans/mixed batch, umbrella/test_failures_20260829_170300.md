# Test failures 20260829 170300

- [x] Locate the report and read repository instructions
- [x] Classify the five failures and two timeouts from report artifacts
- [x] Trace each result to the responsible test, code, or environment condition
- [x] Summarize root causes, confidence, and recommended next actions

## Findings

- `test_launcher_dpad` and the D2 half of `test_ogl_runtime_texture_options_unified` still expect `Define Controls`, renamed to `Controller Mapping` in commit `8c3832cd`
- `test_input_demo_regressions_d1_graphics_canary` was slowed by ordinary replay diagnostics writing a 5 MB `ai_schedule_probe.log`; the same demo passed in 68 seconds in the primary run, then the canary exceeded 180 seconds
- `test_input_demo_regressions_d2` never reached a replay; a focused rerun spent about 460 seconds configuring dependencies and 338 seconds running the demos, leaving too little margin in the 900-second suite timeout
- `test_level_metadata_benchmark` suffered host process-launch delays while analyzer-reported work remained 0.00 to 0.02 seconds; its focused rerun passed in 30 seconds, while its clean-build subprocess allows up to 165 seconds inside the old 180-second suite timeout
- `test_android_saveload_dispatch_unified` intermittently hung in D1 immediately after the final quick-load request; the identical D1 sequence passed earlier the same day and D2 passed in this run
- `test_obsidian_level7_switch_guidance` selected the expected shoot-switch goal and trigger 7; a live probe confirmed segment 102 is the planner's reachable firing position and the target still matches the activation, so the old segment 304 assertion was stale

## Implementation

- [x] Update launcher tests for the `Controller Mapping` label
- [x] Gate ordinary replay diagnostics so regression runs do not write hot-path probe logs
- [x] Revalidate D1 quick-load after a cold emulator restart
- [x] Increase suite timeouts to cover the measured demo-build and benchmark envelopes
- [x] Align the Obsidian assertion with the route planner's certified firing segment
- [x] Run scoped format/lint plus focused failure reruns

## Verification

- D1 graphics replay canary passed in 8.3 seconds
- D2 headless replay suite passed all 11 demos
- D1 save/load dispatch passed through the final quick-load
- Launcher D-pad and unified D1/D2 OGL tests passed
- Obsidian level 7 switch guidance passed all 47 actions
- Level metadata benchmark passed all 11 levels
- Android debug assembly and Windows D1/D2 host builds passed
