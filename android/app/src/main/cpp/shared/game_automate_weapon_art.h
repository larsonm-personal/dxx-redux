#ifndef GAME_AUTOMATE_WEAPON_ART_H
#define GAME_AUTOMATE_WEAPON_ART_H

#include <stddef.h>

/* Debug-only real firing, save/restore and GPU readback in the loaded mine */
int game_automate_weapon_art(char *reason, size_t reason_size);

#endif
