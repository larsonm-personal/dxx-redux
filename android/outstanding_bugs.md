copilot/codex note: do not edit this file

# bugs
[ ] guidebot is still navigating to a specific point and freezing. for example in d2's final level (I think level 24 (?)), when navigating to the yellow key, guidebot goes to a point right above it and stops. this is different than the base game behavior
[x] I saw pretty bad slowdowns in d2's final boss during a coop game. it's a big area with many earthshaker missiles (which seem particularly expensive), etc. but it should run better. see these logs which hopefully contain slowdown logs: "C:\Users\first last\Downloads\debuglog_20260829_101315.txt"
[ ] secret areas which are behind fly-through lava/water are not parsed as secret areas. for example the one in the floor near the start of obsidian level 13
[x] I see the same problem with obisian level 12 as we looked at yesterday. your changes had no effect, guidebot still goes back near the start when getting stuck on the "next" objective, makes no attempt to get closer to the switch in question, or do anything helpful. is there, in fact, no way to navigate to the switch objective? obsidian is a mildly buggy mission, but I actually doubt this. can you look at these logs:

"C:\Users\first last\Downloads\debuglog_20260827_140429.txt"
[x] in the save explorer - the name of the active mission (IF not d1/d2) should be more prominent in each save prsentation. maybe the very first thing. in the "save set" view, "level set" selector should be moved to be 2nd from left, after "game: d1/d2"
[x] "define controls" -> change text to "controller mapping. reduce edge region highlight intensity by 40%. mouse mode touch region - I'd like the axis labels in the touch overlay, the ones at each edge, to be the names of the actual axes instead of "stick up" "stick down" etc.
for touch sticks, I'd like additional axis labels, visible only when in the touch editor (like the edge of mouse region labels), which show the axis directions. the same as for the mouse mode region ones. they should be like "strafe left" and "strafe right" etc. (strafe - and strafe + would be fine if that maps better to existing axis text routines)
[x] want to see edge regions highlighted as partially-colored regions (with color intensity relative to movement %). yellow for left-right and green for up-down. with full-green/yellow (solid color) for 100% regions, which should highlight the screen edge standoff regions. text labels for stick up/down/left/right
[x] go through powershell scripts and see if they can be made 5.1 compatible without too much trouble. I don't want to lose expressiveness or detail handling or correctness. just stuff like, could it be 5.1 if the quoting rules were respected better? etc.
[x] reactor pause has no or very delayed reaction in multiplayer
[?] autosave not available in multiplayer (why? it should be)
[?] in d2 level 20, after the boss, guidebot attempts to navigate to the exit on a path that is blocked by a non-passable grate, same as guidebot was trying to do when bypassing the yellow key
[x] with the addition of the "paused" indicator, we are now requiring extra unpause taps for certain menus. for the overlay settings menu, I'd like closing that menu to unpause (currently it's one tap to close the menu, plus a 2nd tap to unpause). the pause icon should be thought of as 1. an actual pause guard for certain menus (rare) 2. an indicator only, with the previous pause/unpause logic staying as the controlling thing
[x] I'm getting a pretty large in-game freeze when new CD audio tracks (from the macplay image) are started while playing obsidian. I'd like for there to be a way to background whatever difficult load is happening so it doesn't cause a framerate drop
[x] I'd like a kotlin overlay to make the menus better (the main game menus that are shown when not in a 3d game environment). I'd like to allow pinch-to-zoom (as long as *both* pinch points are not on tappable/scrollable items), plus panning up/down (I'd like to hold off on left/right panning - let's assume we keep everything centered left-right, including across pinch-to-zoom, which will not translate left-right but could translate up/down as part of the zoom). I'd also like to permanently show a "back" button, bottom right, circular, and maybe 2x the diameter of the current "exit" button sometimes in upper left. it might make sense to make this stuff composable with the "exit" overlay which is on a few out-of-engine menu views. please look at a plan for this. it should account for accurate taps/game menu scrolls (remember, those are a currently-supported very special handling item), text entry, and so on, and still retain the "center text entry when opened" bit, while zoomed. all of the math should be cleanly recalculated and still work for all of those elements
[?] it's marginally better now, but still a noticeable freeze (1/2s maybe)
[ ] gamepad navigation of game menus
[ ] touch navigation of game menus
[ ] pinch to zoom in game menus
[ready to test] there's a (what feels like) new 1-2 second delay from pressing "load save" in the launcher to getting an in-game progress bar. can you investigate this? and if it's a required action, can it be made more efficient? and can you add a progress bar that appears in the launcher for it, or some other visual feedback that things are being prepped to load the saved game before the engine starts?
[x] level 19: can't reach blue key (initial goal). guidebot getting stuck too easily when an impassable door is in between, even when it's a door like a secret door that isn't locked or a red/blue/yellow key door that we have a key to. it should navigate to the door that's in the way
[x] "marker 9 not placed" when repeatedly instructing guidebot exit target. guidebot spinning and not navigating to exit, getting stuck, significantly degraded from a few versions ago
[x] (from the legacy of chaos readme): "Statistics: 518 cubes, 115 walls, 16 triggers, 229 objects, 53 textures." - add a version of this to the metadata view for each level, as a header. I assume it comes out of one of the 2000s-era level editors
[ ] try "outbreak 2" (does this refer to "bahagad outbreak"?) which was billed as a "total conversion" which thus may exercise more features and demand more of the metadata browser and so on
[ ] having downloaded a bunch of d2x-xl missions, and done an initial sort on some that might be new/not in the database and playable, continue analyzing them to pull out the ones that are truly different
[ ] a way to unify all of the options and make them searchable
[ ] options preset: 1. a preset that's as close to the stock game as possible (no extra cheat options, no guidebot line, and so on) 2. my preferred default
[ ] saving paused the game with no clear way to unpause
[ ] guidebot routing stuck on obsidian level 6 at "5: shoot switch trigger 14" - guidebot wants to navigate through a closed grate
[ ] net events view seems to appear when in multiplayer in map mode, even when it's toggled off in the main view. it also appears during loading screens in inter-level screens, which is good, but I don't want it on in the map screen
[x] my phone which I've used for testing for many weeks seems to have levels that aren't associated with any mod, which are being processed in the metadata scan. in fact, my "storage inspector" view shows many many files that don't make sense to me, 16k files now. can you add some logging so that I can export the storage inspector files list (and saf links list) so you can analyze it?
[x] include chromaprint hashing in the background tasks along with level metadata generation, and have a 2nd progress bar in the advanced tab showing its progress (can have the same running log). chromaprint should be done after level metadata is complete for a given mission
[x] metadata viewer for mp3/ogg/flac. finish out midi metadata viewer. backup source for track info while playing
[ ] review external update notes for completeness and accuracy
[x] convert all .json5 to .jsonc since vs code comes with a parser built-in for .jsonc and all we need is comments anyway
[x] kcxf2 chromaprint decodes for the bundled .oggs decode to mechanical names rather than, well, I thought we had one of those curated files with the actual track names (?). yeah, we do: game_data\mission_files\KCXF2RMv11.tracklist.json
[x] why is "maths/cmakelists.txt" favored as an apparent dumping ground for added files? I haven't dug into it but it looks like a sub-makelists rather than the appropriate level to put most of this stuff
[x] a recent error message on android showed "C:\local\dxx-redux\...\some_c_file.c" as a path within the error message. can these be santitized somehow, to make the paths relative to project root maybe?
[x] boss robot health bar appears only after the boss shoots a player or a player shoots the boss, including invulnerable hits
[x] android\ai tool plans\CD, installer parsing\on_device_mac_extract.md move ball down the field on this
[x] coop post-level screen: "press esc to skip or wait N seconds" should allow tap/"a" controller button to skip, like we do on other similar screens
[x] multiplayer helper view should be hidden by default, or brought in with a swipe
[ ] menus brought in with swipes: configuration in touch overlay
[ ] join approval and warp button pop-ups should have a configurable location in touch editor, or default to the top edge of the screen
[x] currently we have a coop QoL setting to not have player death spew on a timer (it lasts forever). it seems like when coop save games are loaded, death spew then has a timer on it even when the setting was set (it loses that property)
[x] move found games up in the multiplayer view (to the top of the screen, rather than the bottom) so that they're quicker to see. rather than having to scroll down. there will normally be only one, the happy path should support a single LAN game being quickly found and joined
[ ] opengl efficiency. for example, we already did a "don't switch palettes between characters in a string" and "batch draw lines in automap" got huge gains from both of those. there's probably a lot more out there
[ ] PR review prep
[ ] guidebot improvements (from tvtropes):

"Annoying Video Game Helper: With Descent 2 having more complex level designs than Descent 1, the addition of a Guide-Bot seemed like a good idea to help players not get lost. Unfortunately, the Guide-Bot is very easy to lose track of because of its fast flight speed and sudden, erratic movements. If the Guide-Bot comes back and sees you doing literally anything except actively following it, it will shoot flares at you to get your attention, sometimes damaging your shields in the process. Most players will leave the Guide-Bot in its prison cell and stick to using the Automap.
Descent 3 made some changes to the Guide-Bot to alleviate complaints fans had with its Descent 2 iteration. Its flight speed and movements are largely unchanged, but now it tries to avoid hitting you with a flare, and apologizes if it hits you by accident. The Guide-Bot being stored in the player's ship allows the player to deploy and recall the Guide-Bot at any time; you can just keep it docked and ignore it if you wanted to. The drawback is that now the Guide-Bot has collision physics, so it is possible for it to block your shots and ram your ship hard enough to cause Collision Damage...which it will if you recall it, because it enters its cargo bay by slamming into your ship at full speed."
* attempt to not hit the player?
* ability to recall/ stow?
[ ] progress text (upper right, robots n/m, secrets j/k, etc.) disappears when guidebot text is printed
[x] "next objectives" in automap. list in upper left. all should be red color. have it be the current next, and up to two after that. same text as the other automap text
[x] kcxf2 level 5: guidebot flies through a not-passable tunnel (too small for the player's ship, barely) in its route to the "fly through" objective after the red key
[x] kcxf2 "scanning levels" freezes with no progress at step 3/5 (note: there are 6 levels plus 2 secret levels)
[x] kcxf2 "show objectives" for level 6 shows objectives "1" and "2" only in the automap. level metadata shows objectives 1-7
[x] kcxf2 level 6: the blue key is within a robot that needs to be blown up. I'd like for that to be correctly identified. in the automap, it should also be a moving objective (showing the robot's location) if objectives are turned on. there are a few descent 1+2 levels that also have key-containing robots. I'd like the guidebot messages to also say it's a robot to be destroyed rather than just "finding blue key"
[x] automap "show objectives" control: should cycle from off (default) to "all" to "remaining" to "next" (only the next one) (and back to off)
[ ] hide online multiplayer stuff. just do a pure lan or direct connect version
[x] multiplayer joining with loading a saved level *still* has problems
[ ] with the new extracted mission zips process, the music preview files fail at previewing. my test case was with kcxf2rm
[x] progress bar when loading mission zips or 7z or rar files (maybe it's only 7z files that are missing this). currently the launcher gives no indication after the files are picked that it's working. put the progress bar in the same spot where the "imported..." message will be printed
[ ] multiplayer coop games are *still* loading bogus savegames on game start, even when "start fresh" is selected. nothing I try is able to start fresh the way I want to. you attempted a bunch of fixes for this last night, I don't notice any improvement. yes I'm running the updated code
[x] death screen (losing all lives): if keyboard is closed on the "enter saying" page, you get stuck there
[x] multiplayer: some level textures messed up (in d2 base game)
[x] multiplayer: connection error dialog has messed up palette/text (in d2 base game)
[ ] multiplayer: resume saved level isn't working, the 2nd player doesn't join. it was working briefly in the past but not now
[ ] d1-in-d2: work on identical weapon stats (spreadfire for example) as part of a longer-term goal to get demo compatibility and delete the d1/ folder. `android\ai tool plans\gameplay\plan_d1_in_d2_gameplay_semantics_survey_20260614.md`
[it does] does the text "cloaked" appear still like it does in the base game, when in fullscreen mode? if not, needs a fix
[x] in the level metadata browser, add a feature that can preview the full level (wireframe) in the automap view. this would launch the game in a special mode that would go straight to the automap (with the game set up to display that and exit when done, not do anything else)
[ ] find and include in releases: "d1 weapon damage mod"
[ ] include guidebot in descent 1. this is a someday thing after everything is merged. UI support can be from the existing "deploy" option in the guidebot wheel
[x] for levels without >N coop starts defined, I'd like to allow >N players and just place them all in non-overlapping spots at the same single start point (or N start points). this may already be implemented in rebirth's code - can you check? and if not, you can use something like the placement code that was added for the warp-to coop feature, but targeting the single start point
[x] the left and right icons in this screenshot look neat, plus the enemy health bars: https://sectorgame.com/dxma/mission/?m=51 - are those already in rebirth? or are they d2xxl features?
[ ] rear view controls, like most youtubers have set up. make sure it's 90 degrees or whatever the default is, not expanded based on the fov setting
[ ] multiplayer lifecycle, join/resume/rehost etc. helpers, and progress loading helpers like a quick resume system. this is all partially implemented but needs refinement based on play testing
[x] move the secrets cheat from to the other overlay menu (?)
[x] cheats menu in overlay menu. remove the existing overlay cheats menu
[pretty good but needs refinement based on play testing] can you do a deep study on the problem of possibly auto-labeling secret areas in levels, such that I can track which ones have been found (a count, like 1/8 found)? we would define secret areas as any areas that are reachable by the player that are *not* necessary for ordinary game progress (don't contain hostages or keys, and normally don't have robots) behind doors that are of the hidden door type, that have regular textures, like rock textures until shot (not sure if there's a good way to categorize these)? the idea would be to scan a level for secret count at load time, and then make a generated list of these (which should probably be cached somewhere), plus a cached "has player gotten here" list. I expect that it wouldn't be perfect. a cheat code could then show them in the automap, as well
[x] if I launch a save game from the launcher for a mission set I don't have in the mods list, I get "error unable to load mission '[mission name]'. that's expected. the bad thing I see is that the background outside of the error box is white/black/gray snow/static. can you see why, and when you figure it out, audit the rest of the game for similar static background susceptibility?
[needs testing] enemy within 15th anniversary support: please see this file: "dxx-redux\game_data\ewithin-versions.zip". it has inside a rebrith and a d2x-xl version (both a sub-zips). we need the rebirth version. then inside the rebirth file are a bunch of other files we need to support. please see this description of its changes: "An original industrial rock soundtrack featuring a mix of real instruments, real synthesizers, and the occasional VST plugin, and which, in the style of games like Devil May Cry and Sonic Adventure 2, mixes instrumental tracks with a few vocal songs that are sprinkled in to highlight especially cool moments.
New sound effects for all robots (except thief) and actual voices for all the warship pilots, featuring myself, Pumo, and Regolith247 on V/O.
A storyline that's been adjusted just enough to keep even returning pilots guessing.
Full exit sequences for almost every level, made by Parabolicus to slot in perfectly with each map's theme and aesthetic. Unfortunately these will only work on recent versions of D2X-Rebirth, as no other version of Descent 2 supports them.
Two separate versions, optimized for D2X-Rebirth and D2X-XL (a D2X-Rebirth custom install is the recommended experience).
A few minor tweaks to gameplay.

What's been taken out:

The mission no longer supports DOS Descent 2 at all. Exit tunnels push the segment counts for many of the levels above the vanilla limit and several of the maps never really worked properly in the original Descent 2 anyway.
The midi tracks have been removed 'cause I couldn't figure out a way to keep them in without having that interfere with the new content."

please come up with a plan to support either the parent zip directly (and choosing the rebirth sub-zip) or to support the rebirth zip as the loaded file. both are big enough they should be extracted vs. being loaded in-memory
then inside the rebirth zip, go through its contents and come up with a plan for how to support it. it may have slightly different paths etc. than previous mods. please look at the features listed and see if each of them is supportable. the mention of "vst" has me concerned since often that means there's x86 binary code involved, but if it's just descent engine data files then it should be ok
[x] the mn2 preview for obsidian.zip is showing as non-scrollable for me, even though it looks like its last line is beyond the viewable area. same for obsidian.hog (its last line is "contents preview" but the "p"'s tail is cut off)
[needs testing] please review obsidian.zip, which is a descent 2 level pack. I want to make sure we can support all of its features: it has a midi soundtrack (which should be activated as the default sound mode the first time it's opened). also, I think I want to save the default sound mode into save games, so that if a player is switching between base d2 (where they'll probably prefer CD audio) and a level pack with midi (for example), their save games will set up that option on each switch. it also has custom enemeies, custom textures, custom player ship and HUD overlay, and custom briefings, and "non-standard" level objectives. please inspect the zip contents, do a survey of how all of this functionality would be activated, make sure we can support it, then also do a survey of our mod metadata support and see if there are opportunities for enhancing that to show any information from the level pack that we don't currently show. planning only. zip is here: "dxx-redux\game_data\Obsidian.zip"
[x] level pack zips frequently have a .txt in them. when there's a .txt in the "files" section, 1. move it to the top of the list, 2. tapping it should open the file details pop-open, which whould have a button in addition to "close", which is "view" and 3. if there's a single .txt file, show "view readme" in the upper right of the base details view for the .zip, to the right of the mission name, which will open it for viewing. for the initial "view" option I'd like it to be an in-launcher text display, we might move this to a share menu option (which would use an external viewer) if this doesn't work right 4. if there are multiple .txt files, then if there's one that's named README.txt, use that for the readme, or if not then if there's one that starts with the name of the zip, then use that, and if not, pick the largest .txt file (byte size) and use that
[needs testing] QoL feature: in descent 2, allow spawning the guidebot from the player's current position if it hasn't been released yet (which would remove it from its cage), or it wasn't added to the map by the map maker (which would create it). have it be an option when the guidebot wheel is in "locked" mode, if pressed and dragged to the outer ring, let's say the outer 1/3 of the normal full-open ring size. that region should have borders drawn, should enlarge on highlight, and have active highlighting just like the normal wheel pie regions
[x] energy-> shield action in the "unbound actions menu" needs to print an in-game message (the ones that pop up at the top in green text) if that powerup isn't equipped
[x] "10 most recent" saves in the new save explorer seems to include duplicates, a couple auto saves are repeated in my testing
[needs testing] more auto saves: auto save every 5 minutes, and have two slots dedicated to that. so one slot will be 0-5 minutes ago (renewed every 5 minutes when it expires) and the other slot will be 5-10 minutes ago (the previous save). like a ring buffer. slot A or B is overwritten when the newest one goes past 5 minutes old
[x] don't use "sectorgame" in code or filenames anywhere. just call them "mission zips". please fix this across the board. you'll need to rename a few files. when done, the only places "sectorgame" should be visible in a full text search should be in planning docs
[x] I'd like a little bit more metadata support for common game files, such as .hog/.pig. what awareness can you add so that the info pages in the launcher for these shows their general contents, scope, editor, built-in metadata, etc.? can you do a survey? start with the files in the uneasy zip. planning only for now
[x] I'd like to support missions downloaded from sectorgame.com, *without extraction*. these are typically distributed as zip files which contain something like a dxa (mod zip), mn2 (mission file), and hog (assets). can you sketch out how you'd support importing these? I think they should go into the mods list and there should be a basic rule that categorizes them as levels. is it possible to enumerate which game they're for based on examining the mn2? then they could flag whether their base game assets are present. in the info popup (from the launcher's mods section), they should list their constituent files, and allow info popups for the constituents as well (with the same info in these sub-popups as we have currently if a user were to import an mn2, hog, dxa by itself). if they're small enough, let's say under 100MB, then they could be extracted in-memory and have their constitutents presented to the game's physfs. over this size limit then offer to extract into game data and manage them like we manage extracted gog CD images - where deleting the top level entry deletes all, and they're linked as far as the "advanced" tab's file/storage/link inspector
- I've placed an example file here: "C:\local\dxx-redux\game_data\levels\Uneasy4.zip", this will be the prototype that needs support
[possibly fixed] audio sound effects latency needs a holistic look, it's about 1/3 of a second delayed on my very new phone in descent 2. it has been better than this in the past but i dont' think anything important has changed, it may just be inconsistent. I want to minimize it to the greatest extent possible. please survey all possible causes (within the android app)
[x] when saving a game, suggest the level name like "level [N]" as a pre-filled string, and ensure the android keyboard is able to start with that text such that hitting return accepts it
[x] reduce automap translation speed by another 30%
[x] "jump to marker" in the automap should only show actually-existing markers as jump-to options
- also, "drop marker" should open a sub-menu showing unassigned marker spots, rather than its current behavior which is to present a text entry cursor. I just want to name the markers 1-9. we can have a 2nd quick menu option which is "name marker"
- "set marker" in the automap should work the same way
- also, put a text label on top of the marker (similar to how text labels are drawn on debug texture overlays). the text color should be dark red so it shows on top of the green marker
[x] in-game FoV slider. I'd like a slider, similar to how we have a brightness slider. the reason is that many phones are 16:10 or even wider and have poor vertical FoV, so they need a little help, and I'd like to provide that help by allowing a wider FoV which would scale to both horizontal and vertical dimenions (things would still be square). I'd like the base option to remain what it is (is it 90 degrees?) and the slider snaps should be 100, 110, and 120 degrees
- I *do not* want any game behavior to change - I can see an immediate hazard which is that if robots are drawn in the >90 degree regions which are now visible, that may wake them up earlier than they should, and demo compatibility will break
- don't apply the FoV change to the automap, or to missile HUD views, or anything other than the main 3D environment view
- planning only: please research the current fixed FoV system and research all of the areas that would need to change to have this FoV depend on a single global constant, which we could then edit
- when it's edited, I'd like to have it be a setting in "graphics" settings, and saved/exported/etc. similar to those settings
- allow multiplayer non-coop servers to restrict FoV slider use to the base FoV (prevent clients from adjusting it). this won't be secure in any way, just a gentlemen's block. persist the preference for this setting along with other persisted hosting options
[x] in-game difficulty slider (which would allow continued progress - it wouldn't redo the starting conditions of the level, just change what can be changed while playing, then on the next level's start the difficulty would be fully the new difficulty)
[ ] an overlay just for the in-game menu, which shows a d-pad and a/b buttons (visible only when there's an android touch interface)
- on the left side, four buttons which are labeled with arrows (up/down/left right), on the right side, vertically laid out, button "A" and button "B". go ahead and sketch those out yourself and then I'll make size and position corrections after they're placed
- these buttons should be hooked up to d-pad and controller a/b so that they're able to help navigate the menus, especially the menus with smaller buttons, but shouldn't interfere with touch navigation
[x] in the in-game menu, while in-level, tiny text such as the individual lines in the "keys" menu are drawn too small to see. this is even while the actual phone screen's resolution *would* be high enough to see the text - it looks like some intermediate rendering spot is very low res. also, the text is just plain too small, it should be zoomed in, and individual lines reflowed to an indented 2nd and 3rd etc. line as needed
- please look for an intermediate text rendering resolution fix first, at least then the text will be readable on most high dpi phones, but too small
- this applies also to the text in the keyboard controls editing, and other controls editing views. those will be more difficult to scale text on, so maybe the entire menu needs to be taller (and wider - zoomed in up to 85% of screen width, insted of capped at 85% of height) and scrolled in from below screen
- also, to the in-game "find net games" menu - text can't be read
[x] the in-game autoselect ordering menu: should have two ways to select an item in order to have it selected for moving up and down: long-press (touch) or a long-hold of button "a". button "a" or button "b" should deselect and drop it in place; for touch, it should be dropped in place once the long press and hold stops. this would require also a touch interface to drag to the new location
[ ] sandbox needs touch and controller controls (lowest priority)
[possibly fixed] the in-game "load game" menu doesn't show screenshot preview images anymore. it's probably related to the out-of-game (launcher) preview image rework
- I'd like them shown whenever a given save is highlighted (which could happen with the d-pad navigating to them, or with a long-press and cursor move that doesn't end up selecting them)
[needs testing] show current ammo status (count for missiles/bombs, green/yellow/red for lasers and gauss/vulcan) in the quiescent button state for the weapon selector pop-open (circle touch menu), for the currently in use primary/secondary weapon
for lasers: green should be anything > 70 energy, yellow > 30, red < 30
for gauss: green should be anything > 5000 rounds, yellow > 2000, red < 2000
these two aren't shown as a numeric value because that info is already on screen elsewhere, but a green/yellow/red value is useful
all other weapons should have green/yellow/red status (which they already have in the wheel) plus their counts, in the quiescent state
make these constants easily findable and make them the same for d1/d2 (a shared constants file)
the green/yellow/red cutoffs for all other weapons should also be centralized into this shared file
for descent 2: currently, the secondary select touch pop-open circle menu doesn't show both ammo types, even though it collapses for example homing and guided missiles (which are, in the base game, selected using a single key - I forget which, but pressing the key one time will go to the highest priority weapon or the only one left with ammo, pressing it a 2nd time goes to the other, if it has ammo). I'd like to show ammo counts and color status for *the weapon that will be selected when the wheel option is picked*, and ensure that the quiescent state shows the name of the actually-selected weapon and the count for the actually-selected weapon. so, for example, if the user has ammo for both flash and concussion, and has neither selected, and has flash as the higher priority weapon: the wheel should show flash, and the flash ammo count. if the user selects flash, then re-opens the wheel while flash is selected, *now* the wheel should show concussion, and the concussion ammo count
[ ] multiplayer controller navigation: for a phone with controller+touch interfaces, I still want to have the same starting position of the controller selection, if the user happens to use the d-pad to navigate. the green highlight should be initially off, but on a given page, as soon as the d-pad is used, it should be turned on until that page is navigated away from
in the base multiplayer screen (the one with "matchmaking server url" "connect" "lan"), the default selection should be the "connect" button. so if the user's first d-pad action is "right arrow" then the green highlight should first appear on the "LAN" button since it's one position right of the "connect" button. currently, it seems like the default selection is the server URL box, which is wrong for both controller+touch and controller-exclusive interfaces (which should have the same starting green box - "connect" for both)
in the "host lan game" screen, once the "mission" box is navigated to with the d-pad, the d-pad navigation becomes stuck. please allow it to be navigated to, and away from
in the "lan games" view, when hosting, if the d-pad navigates to "restore" and selects it, the d-pad highlight button snaps to "stop hosting", which I don't want. if the user selects a control it shouldn't snap the d-pad selection away at all, it should just stay on that control
please work on a design for this, and fixes across any menus you find, and specific fixes for the multiplayer screens I mentioned
[ ] begin extraction of game-agnostic parts to a reusable library. launcher, CD management, mod management (not physfs stuff), controls editor, crash handler, settings export/import, some multiplayer bits
- I envision this being a combination of kotlin, C code that will get compiled alongside (generic versions of game hooks), headers for the game hooks, C code that supports the kotlin parts (that won't get compiled in - just the things like the parts needed to play MIDI, CD audio, etc.) plus an extract of the scripts and deps management that can reasonably be put into a game-agnostic library
- the game would retain its walled-off portion of the hooks for the library, the customization for the launcher (which would be generic. for example, the game would feed the launcher a list of its controls, probably in json, which the launcher would then offer as overlay or controller controls)
- tests that are specific to the generic launcher should be brought over, as well. some effort should be made to genericize tests, it's ok if we lose some of the descent details on some of them, there are too many tests already
[needs testing] multiplayer rewind support (gated by qol features)
[needs testing] multiplayer quick "last server/host/save game/etc." resume feature - offer when first opening the multiplayer submenu
- if last multiplayer game was hosted, create a new host setup with the most recent save as preset
- if last multiplayer game was as client, scan and also ping the previous server's IP for a possible shortcut to get the server to appear
- the initial use case is two players who habitually play coop games together on LAN. I want to give them one button to press to get reconnected and set up to get all the way back to in-game with a saved game loaded
- you might also want to do a survey to make sure we have multiplayer saves being taken at all of the appropriate times (minimize, checkpoints, etc.) and that they don't overwrite single player slots
- in a future bit of work I'll want to add multiplayer rewind support, so think through that too
[ ] controller interface to edit the controller-specific touch overlay (which is view-only, and choosing the locations of the menu buttons)
[needs testing] multiple controller and multiple touch config slots:
I'd like to add the concept of multiple controller config slots, and also multiple touch interface slots
in the "define controls" controller editing menu, I'd like two new buttons: 1. choose active slot and 2. apply preset to slot
1. choose active slot: opens a sub menu (which you will share with the touch interface - although each one gets its own independent set of slots, just the menu to choose/edit will be the same). this should have a name field (text edit field), new slot, delete slot, duplicate slot. the [0] slot should be named default and not name-editable or deletable. duplicate current slot shoud prompt for a name for the new slot, with ok/cancel. delete slot should have a confirm window with ok/cancel. new slot should prompt for a slot name with ok/cancel. the overall slot editing window should have a close button
2. apply preset: this would take one of a set of built-in presets (to be defined - for now, use the default controller preset that's already within the project, or the set of touch interface presets for the touch editor version of this menu, which should be shared again but have different backing data)
B. touch interface version: we already have a presets button. add a slots button that opens the slot editing menu, to the left of the presets button
N. when selecting "export all configs", the config should be changed to include an array of slots for each of touch and controller configs, and export all of the configured slots. import should support importing and overwriting all of them, as well. export/import in the controller page should export/import only a single slot (the current one). same with export/import for the touch interface
M. the current slot name should be top-center (above the controller image, but done in a way that doesn't shift the controller image or anything else down) in the controls editor, and bottom left in the touch editor, in very small text, again in a way that doesn't shift anything. both should have a 25 char name length limit
[ ] a specific default touch overlay for controller-only systems
[ ] mods - clean up mod offers, and do a release with high res and cleaned up texture packs. clean up analysis/info of mods. ensure high res and cleanup packs conflict
[done] swap "stop showing this" and "resume recent save" in the recent save pop-open. style "stop showing this" as a button like the "load last save" buttons (but still with its smaller text/smaller size to fit on the line with "resume recent save" and the thumbnail)
[done] in the LAN game menu, if I'm scanning for games, then minimize the app and restore, I'll sometimes get the warning "broadcasts failed, check permissions (paraphrased)". can you make this self-healing? it should be able to fix itself when resuming the app, and could maybe benefit from awareness that it was de-powered/resumed
[done] on a phone, with a touch interface, the green button highlight is sometimes briefly shown when going back to the main launcher page from a sub menu. look for the reason why and try to fix it. it may also be happening on other menus and I can't tell
[done] remove the "your ip: ..." line below the LAN games line, and move the "join by IP" button up to that new line. then the "host lan game" and "stop scanning" buttons can share a line and fit better. the join by IP button should be made a little smaller to be similar size to the current "your ip: ..." text
[needs testing] "exit" overlay button has no effect in some cases (for example, a waiting player screen for multiplayer), and I think also during cutscenes. it may have no effect at all anymore, I can't tell. can you take a look?
[done] "start game" green overlay button is now selecting/deselecting players from the "select up to N players" screen rather than actually starting the game. some signal may have gotten mixed up during a recent menu refactoring
[improved, needs testing] auto save thumbnails are still not shown sometimes. it could be because they're not created well enough during program exit, or could be that the save thumnail reader is flawed. either way, I'd like to make thumnail failures more rare
[needs testing] cutscene videos sometimes play with corrupted borders (when the video doesn't fill the entire screen). can you look into that? it might need the borders drawn on each frame, or zeroed before the video starts
[needs testing] player select menu *still* doesn't register taps at the right locations. or, maybe the locations are accurate, but taps don't register. it's frustrating to use and could indicate other problems
[needs testing] when flying out of an exit in d2 with touch controls, generally the exit video is immediately skipped. I need some way to prevent the currently-held controls from skipping the video until all taps are released, then at that point the next tap can go ahead and skip the video. it may be useful to have a 1/2 second cooldown like there is for, I think, controllers
[done] gamepad select button should open the overlay settings menu, not the in-game menu. maybe the in-game menu doesn't show when we have the touch overlay off?
a. also, the select button (on android tv) should cycle between opening the "more" menu (if shown) and the overlay settings menu. when one of these menus is open, "b" should close and return to normal game control, "a" should select an item or open submenu etc.
[needs testing] mouse mode for the touch interface in-game look axis: it now has a *significant* deadband that can't be turned off (even with curve=linear). this is new as of sometime in the last few days
[ready to test] when loading a mod and close to device storage capacity, this can cause a crash (with no crash dump). 1. need kotlin crash dumps working in this case and 2. check for free space when extracting things into game data
[fixed] on regular android, an imported "infinite abyss" CD (which is reconstructed into a game data folder single image file) plays as static. other types of CD audio work. the audio plays correctly in the launcher's track list player
[fixed] on regular android, the main loading screen and menus are back to having texture filtering, even though the setting for that directs it to be off for these textures (as it is for in-game menu textures). the setting is "enable filters for..." "menus / briefings / videos / text / reticle" in the graphics tab
[fixed] android TV: d2 gog installer is failing to extract the CD image, it skips forward instead of extracting it. might be failing with limited memory or something, need logging
[fixed] android TV: can't multi-select files in "import" saf picker in order to have bin+cue selected for import. any press, long or short, of "a"/"select" immediately selects the single file under it
[fixed] regular android: at "new game" "you may start on any level..." screen: the numbers-only keyboard wasn't filling the text box even though backspace sometimes worked
[fixed] single axes and stick axes *both* fail to show the current axis value in the control edit submenu when they're being edited. they *both* just show the last value (which would normally be 100% because someone opened them with a long-hold)
[fixed] descent 2 preview movies have skips in their soundtracks
[fixed] can't select menu items with Bluetooth controller “a” or with tv remote center of d pad button. stick as options-misc options-automatically start single player demos. this is a checkbox that isn't selectable. the base menus and start game flow work with these buttons, not but items in these sub menus
[fixed] controller on android TV, with the default "B" to fire secondary binding, is sticking and firing all missiles in one go when "B" is pressed. this may be stale - on my last test, the "B" button didn't fire anything. a recent code change changed berhavior to mostly ignore the "B" button (never fire)
[fixed] in - game “b” button press causing a stuck secondary fire button: still a problem
[fixed] high scores menu needs to respond to "b" controller button the same as other menus (to exit it, back to the main menu). actually, this applies to apparently all menus
[fixed] brief "b" button presses are ignored in game menus
[fixed] android TV shows a generic controller name as the detected controller rather than the name of the first bluetooth gamepad. usb gamepads on regular android show their names currently
[fixed] d1 load game preview snapshots are corrupted (and also d2, even after the previous round of changes). could be some kind of dynamic palette thing that needs a special -> bitmap procedure
[fixed] in the android TV dxx launcher, only the d-pad can move the selected button around. the convention in other apps and in the system menus is for both d-pad and left analog stick to move around menus
[fixed] in-level pause and game menu (save/load/quit) have filtering applied even when the graphics settings have filtering off for menus
[fixed] on android TV, and maybe on non-TV, multiple debug logs are now being produced for a single app run. at one point they were combined into a single file per run. this is as of 22 May and seems to have changed in the last couple days
[fixed] android TV keyboard doesn't shift game menus up to keep the text input field visible like we have for regular android on screen keyboard
[fixed] level select game menu should be cancellable with back button, controller "B" button, etc. same as other menus
[fixed] android TV doesn't use the gamepad for the on-screen keyboard. only "a" works as a button press to select the current character, d-pad or sticks don't move around chars

# after reset
[ ] in multiplayer, when on a non-touch device (controller only), whenever the "warp to" or "accept" (allow joining player) touch overlay buttons are shown:
1. ensure they're shown, even if the regular touch overlay is hidden. this may already be the case, I haven't tested
2. add a copy of them to the unbound actions menu. ensure they are inserted into the list at the position that's the first selection when the menu is opened (I forget if this is top or bottom)
[ ] multiplayer item in quick menu: abdicate guidebot. put this in the game settings menu (the one with save/load), and only show when multiplayer is active. when chosen, it will give the guidebot to the next player in line (there's already a mechanism for this somewhere)
[needs testing] possible multiplayer bug: the thief bot doesn't always drop every stolen item, if it's stolen from more than one player (I think, this is just from observations - can you check?) can you see if it's the case that only the owning player's items are dropped, maybe? if so, I'd like a fix. I'd also like its drops to be 100% when multiplayer QoL is enabled, same as the individual player death drop chance modifier
[x] clean up and finalize the demo installer work. this work was done with a lesser model, I'd like you to take a fresh look at it and continue
[x] support the new demo installer packages downloaded after the initial work was done:
"C:\local\dxx-redux\game_data\demo installers\descent2preview.sit_.hqx"
"C:\local\dxx-redux\game_data\demo installers\Descent_demo.HQX"
"C:\local\dxx-redux\game_data\demo installers\descent2preview.sit"
"C:\local\dxx-redux\game_data\demo installers\descent_demo.sit_.hqx"
- this means hashing them for ID purposes, extracting them, hashing their contents, saving notes on their contents in the readme, and ensuring the extraction pipeline for them is supposed in the android code 
[ ] I'd like to have a centralized json file where the name+size+hash of all proprietary test data (CD images, gog installers, and demo installers, basically) is stored, and a support script (place it at game_data/manage_data.ps1):
* a script that presents these options:
 - regenerate the test data json file (with name/hash/size)
 - verify name/size/hash of all test data
 - export all listed test data to a zip file (don't export attendant files - like extracted dirs - just the test data, with its original relative directory structure based against the base of dxx-redux/)
* my goal is to be able to extract the zip file created from this process and have it contain everything needed to run all regression tests or demos

# nice to haves
[done] I'd like a deeper audit of the android extract paths vs. the PC-side extract paths because my goal is that all PC-side supported CDs are also supported on android. there was some recent work done to make the mac CDs work, and to be able to get a clean install from the infinite abyss CD. some of the other CDs will have different kinds of installers. please go through each of the CDs and see if the existing code will do them. you will find some of the history of this work in the android\ai tool plans\CD, installer parsing\ dir
[done] better and less annoying d1+d2 demo install helpers. ideally, .exe processing so the actual original demos can be used
single axis exponential - please add this to the single axis editor the same as it was done for the analog stick editor
remove automap button from settings menu. it should be in the unbound controls menu (if unbound)
if an action is taken in the unbound settings menu (such as activating energy -> shield) and the menu is still open, another settings button push should close the menu
if the unbound settings menu is open for >2.5 seconds, another settings button push should close it (not go to the 2nd settings menu) 
so the only way to get to the 2nd menu is two presses quicker than 2.5 seconds with no action taken on unbound
game menu action from the settings menu does nothing. it should close the settings menu (overlay) and immediately open the game (esc) menu
[x] exponential for analog controls. I'd like a slider next to the deadband slider for analog sticks and also single axis analog editing windows. it should control exponential control (fine/de-sensitized control at low deflection and large control at high deflection) along the same lines as we have with the touch analog sticks. the rnge should be from 1 (no exponent) to N, have N be the same as whatever we have for touch sticks
[x] the unbound actions menu should consider controller buttons also instead of just touch buttons. currently I have rewind bound to a button and it's in the menu
[x] quick save and quick load are really just “open save and load menus” and they should be called save and load
[x] headlight shouldn't be in the overlay settings menu since it's already set up to be in the unbound settings menu
[x] warp and accept buttons shouldn't be in the settings menu for single player. same for net stats and net events, those are grayed out but instead just don't show them for single player
[fixed] af and msaa settings seem to have no effect when changed in-level. texture filtering clearly has an effect. this is on shield where the settings can only be edited while paused. once unpaused, I can see af working. actually, af does have an effect, but it isn't updated live while paused. texture filtering *is* updated live while paused. I'd like all of them to update live if possible
[fixed] af only works on textures that aren't merged, it looks like. this may be new for shield or it may be broken ever since we had the big af merge with the original game settings push a couple weeks ago
[fixed] "import disc image" needs the buttons "extract game files" and "add as audio source" (or "done" - depends on mode) at the top, above the text filename, description, found N tracks, track list, etc.
[fixed] "import disc image" needs "extract game files" and "add as audio source" to be selectable with the d-pad, currently "close" traps the d-pad cursor on open
[fixed] "import disc image" needs scroll indicator icons as was done in other menus
[needs testing] "fire secondary" when bound to a controller button fires all remaining ammo when tapped momentarily. not reproduced by `android/game_scripts/test_fire_secondary.json5` on a fresh D2 default binding, so keep this open only if a config-specific repro still exists
[fixed] the "resume last save" popup needs to be only on the left side column when in landscape layouts
[fixed] "select pilot" menu has the heading ("select pilot") drawn over by the list of pilots (the list starting with "<create new>")
[done] controller binding: support a binding to a "menu" action. *if on a touchless (android tv-type) android, flag when it isn't bound with bold red warning: no menu button*. this button should 1. open the "extra" menu and on a 2nd press, close the "extra" menu and open the in-game settings overlay menu (the one with video settings etc.). a third press should close the settings overlay menu. both of these menus, when open, should take controller d-pad focus and "a" and "b" button focus. the "a" button should select and "b" button either close the menu or exit a submenu back to the top menu if there was a submenu open. the goal is to support accessing all menu items in a controller-only way, and especially to support things that aren't bound
[done] guidebot submenu when not bound (to a touch button, or other button). this should also be a button binding option. it should capture d-pad and "a"/"b" when open. if bound to a button, a 2nd press of the guidebot menu button should close the menu
[ ] some kind of randomizer support, like this, or integrate the code as an option if available: https://descentbb.net/viewtopic.php?t=24628
[ ] need a full re-run of existing tests and cleanup and de-flaking
[ ] d1 regression demos - the demo system was largely worked out with d2, d1 had only mirrored changes. probably has a few bugs left
[ ] the android build system - dependency fetcher, build scripts, test scripts - needs to be ported to linux
[ ] observer mode overlay for demo playback? explore if this would be a good option
[ ] need some fixes for android TV to transition all remaining touch overlay bits to controller interfaces. the immediate need is for the in-game settings menu (the overlay one) to be the thing opened by the select button, rather than the in-game save/load/quit menu (its current function). there was a task at one point to move extra items into the settings menu when there was no touch interface: that needs to be rechecked
[ ] more launcher menus need highlighting. the sliders need green outlines when selected, for example
[done] in-game brightness adjustment that saves next to the af/msaa/etc. settings
[ ] the kotlin portions of the project could use some modularization. setupactivity is 8k lines for example
[needs testing] controls editor needs to have a route to open the edit axis/button binding page for a specific axis/button if it's held for 2 seconds. that would enable quick controller-only bindings setup. for axes, the detection would be looking for an axis held to >80% of full range in a single direction without any other axis going >30% or any other button being pressed (for 2 seconds). for buttons, a button held >2s with no axis >30% or other button presses. only run this detection on the main controller edit page, don't open it for other bindings once a particular binding sub menu is shown
[done, may need minor touch region fixes] resurrect the code that made sure game menus filled 90% of the visible area (or whatever it was) as was done for the software renderer near the beginning of the "cmake" branch (current branch). that code worked great. the intent is to, because this is a touch-first interface, ensure any and all game menus are touch-friendly by being drawn so that their height fills 90% of the visible height of the screen. by default, the menu size gets smaller as the screen resolution goes up, which means it's too small on many newer phones
1. find the original plan files for the existing changes for the software renderer
2. consider how they might be brought over to the opengl render path
3. the math that was worked out for the software renderer was pretty good and took a lot of work. be sure to study it because I want to use it to short-circuit having to redo that work for opengl
4. please study this and come up with the key files and changes needing to be done and create a work plan
[done, needs testing] add support for these sound+textures addons: https://descentbb.net/viewtopic.php?p=338282 https://descentbb.net/viewtopic.php?t=24804 - ideally convert the textures to .dxa so they can be done without patching. sounds are already dxa
[ready to test] rewind support: keep an in-memory buffer of let's say 60 seconds worth of 5 second rewind points. pressing the rewind button goes back one step (with the next rewind save point being added to the end of of the list after 5 seconds after the rewind happens). make the in-memory rewind points a ring buffer or something like it if that will help with allocation (I want this to be, as much as possible, a straight memcpy of all game state. if that's infeasible then it should be a save game, leveraging the existing save/load code. if the existing save/load code is efficient enough, then that should be the #1 option). add a new button binding option "rewind". for now, this will only be available in single player. when a rewind happens, make sure the in-level gametime played is rewound and total playtime is rewound too. this should be an action that is put into the "more" menu if it isn't configured as a button
a. please research how this might be done, what performance impact it might have (it needs to not cause stuttering during play), what memory impact it might have, the best way to do it, the important files and functions, how it might be implemented to share code between d1/d2, etc.
[done] need auto save on minimize. save to save slot 10 and call it "AUTO SAVE": I'd like a system where the game can automatically trigger a save whenever it's minimized, put this in the last save slot (I think that's slot 10). I also want to have an auto save on exit to launcher, put that in the 2nd to last slot. *when the launcher is opened, offer to resume the most recent save (of all player saves: all players in d1, and all players in d2) with a pop-open button in the launcher. it should be "load last save" or "stop showing this" with a setting. "stop showing this" should save a launcher-wide config option that's editable as a checkbox under the "game preferences" section of the launcher (at program install, saves should be shown/ not stopped). under the buttons should be the description: time of save, game (d1/d2) and level, and time in level. to the left of the buttons and description lines should be a very small thumbnail which is the save's thumbnail
a. is triggering this on minimize possible within android? I would like to have this feature even if it involves some compromise because it will greatly help casual play
b. I'd like to also track total playtime for a given playthrough. this means, continuous playtime accumulated from the start of the play sequence that's captured by the save. does this need new info built into the save? when loading, reset this number back to what it was in the save that's loaded. if a player were to play from level 1 through to the end of the game, it should produce a total number, but if they saved and loaded many times it shouldn't count all of the different paths taken in each save/load, only the one actual saved path, if that makes sense
c. present the total playthrough time in the pop-open next to in-level time
please research the design bits for this, important files and functions, feasibility, etc. and propose a work plan
[needs testing] allow buttons to be placed within the drag zones (they currently are allowed, but don't have special handling). if the button is pressed it stays active as long as the drag continues, then the button is released on drag stop. this will give another way to fire weapons while looking with the same thumb
[done] "prepare for descent" loading screen takes potentially up to ~5 seconds with high res textures. it would be nice to show a texture loading progress bar and maybe the text name of the most recently loaded texture or major step, or one text/bar update every 300ms max to limit drawing overhead
